# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**tipsy** — a React app for splitting cash tips among staff.

Planned stack (inferred from `.gitignore`): Next.js, React, TypeScript, deployed to Vercel.

## Commands

> Commands will be added once the project is bootstrapped. Expected Next.js defaults:

```bash
npm run dev      # start dev server
npm run build    # production build
npm run lint     # lint
npm test         # run tests
```
